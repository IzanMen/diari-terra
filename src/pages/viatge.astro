---
import Contador from '../components/Contador.astro';
import LineaVertical from '../components/LineaVertical.astro';
import ElementoEje from '../components/ElementoEje.astro';
import { ClientRouter } from 'astro:transitions';
---

<html lang="es">
  <head>
    <title>Tierra 3D Astro</title>
    <meta charset="UTF-8" />
    <ClientRouter />
  </head>
  <body>
    <header class="header">
        <h1><span>EL SILURIÀ</span><br> TERCER PERÍODE DEL PALEOZOIC</h1>
        <img class="header-img" src="/siluria.webp" alt="Siluria">
        <p class="header-subtitle"><Contador /><br></bt>MILIONS D'ANYS ENRERE</p>    
    </header>
    <img src="/saturno.webp" alt="Saturno" class="saturno">
    <img src="/marte.webp" alt="Marte" class="marte">
    <img src="/venus.webp" alt="venus" class="venus">
    <main>
        <span class="start"></span>
        <LineaVertical />
        <!-- <ElementoEje1/>
        <ElementoEje2 />
        <ElementoEje3 />
        <ElementoEje4 />
        <ElementoEje5 />
        <ElementoEje6 /> -->
        <ElementoEje />
    </main>
    <div class="img-container">
        <img src="/siluria.webp" alt="Siluria">
    </div>
  </body>
</html>	    
    
<script type="module">  
    function slowScrollBy(pixels, duration) {
      const start = window.scrollY;
      const end = start + pixels;
      const startTime = performance.now();
  
      function step(currentTime) {
        const elapsed = currentTime - startTime;
        const progress = Math.min(elapsed / duration, 1);
        const eased = 1 - Math.pow(2, -10 * progress); 
  
        window.scrollTo(0, start + (end - start) * eased);
  
        if (progress < 1) {
          requestAnimationFrame(step);
        }
      }
  
      requestAnimationFrame(step);
    }
  
    setTimeout(() => {
        slowScrollBy(600, 2500); 
    }, 2000);
</script>


<style>
	@import url('https://fonts.googleapis.com/css2?family=Montserrat:ital,wght@0,100..900;1,100..900&display=swap');

    .saturno {
        position: absolute;
        top: 2600px;
        right: 100px;
        width: 600px;
        opacity: .4;
    }

    .marte {
        position: absolute;
        top: 5300px;
        left: 100px;
        width: 600px;
        opacity: .4;
    }

    .venus {
        position: absolute;
        top: 7900px;
        right: 100px;
        width: 450px;
        opacity: .4;
        border-radius: 50%;
    }

	body {
		margin: 0;
		padding: 0;
		background: #000;
		font-family: 'Montserrat', sans-serif;
        min-height: 100vh;
        color: #fff;
        padding-bottom: 9500px;
        max-height: 11200px;
	}

	h1 {
		color: #fff;
		font-weight: 600;
		font-size: 1.7rem;
		text-align: center;
		margin-top: 50px;
	}

    h1 span {
        font-size: 3rem;
        margin-right: 10px;
    }

    .header-img {
        width: 500px;
        height: auto;
        display: flex;
        margin: auto;
    }

    .header-subtitle {
        text-align: center;
        margin-top: 100px;
        font-size: 2.2rem;
    }

    .header-subtitle span {
        font-weight: 700;
        font-size: 4rem;
    }

    main {
        margin-top: 120px;
        position: relative;
        height: 1000px;
    }

    main .start {
        width: 60px;
        height: 60px;
        background-color: #fff;
        border-radius: 50%;
        position: absolute;
        left: 0;
        right: 0;
        margin: auto;
    }

    .img-container {
        position: absolute;
        top: 11200px;
        width: 100%;
        height: 300px;
        overflow: hidden;
    }

    .img-container img {
        width: 100%;
        height: auto;
        display: flex;
        margin: auto;
    }

</style>